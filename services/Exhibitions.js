export const Exhibitions = `exhibitions {
  edges {
    node {
      title
      pageInfo {
        date
        endDate
        timeEnd
      }
    }
  }
}`

// divide into current, upcoming, past

export const Exhibition = ``